/** Значение source общих материалов April (только чтение у клиента). */
export const AI_MATERIALS_SHARED_SOURCE = 'shared';

/** Имена текстовых документов, редактируемых в кабинете (как на бэке). */
export const AI_MATERIALS_TEXT_FILE_PATTERN = /^[\w.-]+\.(md|txt|json)$/i;

/** Ключ кэша react-query для слайса. */
export const AI_MATERIALS_QUERY_KEY = 'ai-materials';
