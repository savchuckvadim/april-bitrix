/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * kpi-report-sales API
 * API приложения kpi-report-sales
 * OpenAPI spec version: 1.0
 */
import type { AiManagerRowDtoLevel } from './aiManagerRowDtoLevel';
import type { AiManagerRowDtoLevelSource } from './aiManagerRowDtoLevelSource';
import type { AiManagerRowDtoSignal } from './aiManagerRowDtoSignal';
import type { MetricDto } from './metricDto';
import type { AiManagerRowDtoFunnelShape } from './aiManagerRowDtoFunnelShape';
import type { AiBucketScoreDto } from './aiBucketScoreDto';
import type { AiManagerTypeCellDto } from './aiManagerTypeCellDto';
import type { AiFunnelEdgeDto } from './aiFunnelEdgeDto';
import type { AiFinanceTailDto } from './aiFinanceTailDto';
import type { AiDisciplineDto } from './aiDisciplineDto';
import type { AiNextStepRateDto } from './aiNextStepRateDto';
import type { AiRiskCallDto } from './aiRiskCallDto';
import type { AiRecommendationDto } from './aiRecommendationDto';

export interface AiManagerRowDto {
    /** Bitrix-id менеджера. */
    managerId: string;
    /**
     * Id отдела продаж по структуре; null — вне ростера.
     * @nullable
     */
    departmentId: number | null;
    /**
     * Id группы внутри ОП; null — группы нет.
     * @nullable
     */
    groupId: number | null;
    /** Уровень: manual — назначен РОПом (settings/save), default — по стажу (< 6 мес. junior, иначе middle). */
    level: AiManagerRowDtoLevel;
    /** Источник уровня. */
    levelSource: AiManagerRowDtoLevelSource;
    /**
     * Стаж, месяцев, от since уровня; null — дата стажа не задана (DATE_REGISTER Bitrix — Фаза 2).
     * @nullable
     */
    tenureMonths: number | null;
    /** Рабочих дней периода по календарю портала. */
    workdays: number;
    /**
     * Старшая карточка «Внимания» менеджера; null — нет.
     * @nullable
     */
    signal: AiManagerRowDtoSignal;
    /** Ключевая цифра: оценка качества за период по звонкам с корзиной (1–10; план руководителя по дням — Фаза 2). */
    keyMetric: MetricDto;
    /** Форма воронки по доле счетов без презентации при не менее 20 счетов: closer ≥ 50 %, presenter ≤ 20 %, иначе balanced; unknown — мало счетов. */
    funnelShape: AiManagerRowDtoFunnelShape;
    /** Три корзины оценок. */
    buckets: AiBucketScoreDto[];
    /** Ячейки по типам звонков в порядке справочника. */
    byType: AiManagerTypeCellDto[];
    /** Рёбра воронки по KPI-фактам. */
    funnel: AiFunnelEdgeDto[];
    /** Финансовый хвост. */
    finance: AiFinanceTailDto;
    /** План CRM. */
    discipline: AiDisciplineDto;
    /** Звонков менеджера в телефонии за период (включая без разбора). */
    callsTotal: number;
    /** Разобранных сравнимых звонков менеджера за период. */
    analyzedCalls: number;
    /** Доля «шаг с датой» за два окна периода. */
    nextStepRate: AiNextStepRateDto;
    /** Риск-звонки периода. */
    riskCalls: AiRiskCallDto[];
    /** Рекомендации (Фаза 2; сейчас пусто). */
    recommendations: AiRecommendationDto[];
}
