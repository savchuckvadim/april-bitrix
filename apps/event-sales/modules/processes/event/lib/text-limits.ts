/**
 * Пределы длины строк, уезжающих в Битрикс (todo2508 №7).
 *
 * Уже были случаи НЕ-записи из-за раздутых значений: название плана становится
 * TITLE задачи (у Битрикса жёсткий предел 255), комментарий уходит в
 * UF-поле задачи и элементы списков batch-командой — километровое значение
 * раздувает cmd-строку до отказа. Лимиты держим с запасом от техпределов.
 */
export const PLAN_NAME_MAX_LENGTH = 250;
export const COMMENT_MAX_LENGTH = 4000;

/** С какой доли лимита показывать счётчик остатка. */
export const LIMIT_COUNTER_THRESHOLD = 0.9;
