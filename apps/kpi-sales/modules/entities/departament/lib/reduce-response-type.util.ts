import { IDepartmentResponse } from "../../report/model/types/report/report-type";

export const reduceDepartmentResponseType = (departmentResponse: IDepartmentResponse) => {
    return departmentResponse;
}
