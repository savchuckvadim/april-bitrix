import type { KpiSource } from '../types';

/**
 * Как читается отметка «учитывается» на стадии при выбранном источнике цифр.
 *
 * Флаг `writesKpi` описывает ровно один факт: пишет ли приложение запись в
 * «ОП KPI». Это ответ на вопрос об источнике «события приложения» — и только
 * на него. Если руководитель смотрит детализацию телефонии, тот же флаг
 * перестаёт означать «работа учтена»: звонки со стадий «Документы» и
 * «Поставка» в детализации есть, хотя записи KPI по ним не создаётся.
 *
 * Поэтому подпись меняется вместе с источником, а не остаётся одна на все
 * случаи.
 */
export const KPI_BADGE_LABEL: Record<KpiSource, string> = {
    events: 'пишется KPI',
    calls: 'звонки в детализации',
    both: 'KPI + детализация',
};

export const KPI_SOURCE_HINT: Record<KpiSource, string> = {
    events: 'Цифры берутся из отчётов менеджера: тип события, результативность, причина недозвона. Длительности разговора в них нет.',
    calls: 'Цифры берутся из детализации телефонии: количество звонков, длительность, эфирное время. Причины недозвона в них нет — телефония не отличает автоответчик от «не взяли трубку».',
    both: 'Объём считается по детализации, результат — по отчётам. Свести их построчно нечем: общего ключа у записи KPI и звонка нет, сопоставление делает разбор звонков и помечает его как подтверждённое или предполагаемое.',
};
