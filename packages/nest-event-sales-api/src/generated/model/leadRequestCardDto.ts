/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * event-sales API
 * API приложения event-sales
 * OpenAPI spec version: 1.0
 */
import type { LeadSiteStatusStateDto } from './leadSiteStatusStateDto';
import type { LeadSiteStageStateDto } from './leadSiteStageStateDto';
import type { LeadOpStatusStateDto } from './leadOpStatusStateDto';
import type { LeadNotCaTypeStateDto } from './leadNotCaTypeStateDto';
import type { LeadRelatedBaseStageStateDto } from './leadRelatedBaseStageStateDto';
import type { LeadRequestSaleReadinessDto } from './leadRequestSaleReadinessDto';

export interface LeadRequestCardDto {
    /**
     * Идентификатор лида.
     * @minimum 1
     */
    leadId: number;
    /** Название лида. */
    title: string;
    /** Лид распознан как заявка (op_lead_site_* или поля лидогена). */
    isRequest: boolean;
    /** Признаки, по которым распознали заявку. */
    requestSignals: string[];
    /**
     * Ссылка «Оценка» лидогена (UF_CRM_LEAD_QUEST_URL) — открывать в соседней вкладке.
     * @nullable
     */
    questUrl?: string | null;
    /**
     * Код партнёра лидогена (UF_CRM_REG_NUMBER).
     * @nullable
     */
    regNumber?: string | null;
    /** Статус заявки. */
    siteStatus: LeadSiteStatusStateDto;
    /** Стадия заявки. */
    siteStage: LeadSiteStageStateDto;
    /** Статус лида. */
    leadStatus: LeadOpStatusStateDto;
    /** Тип «не ЦА». */
    notCaType: LeadNotCaTypeStateDto;
    /** Зеркало стадии связанной сделки. */
    relatedBaseStage: LeadRelatedBaseStageStateDto;
    /** «Не звонить никогда». */
    blackShort: boolean;
    /**
     * Причина «не звонить никогда».
     * @nullable
     */
    blackShortReason?: string | null;
    /** Установлена компания. */
    isCompany: boolean;
    /** Отправлен отчёт в НПП. */
    nppReported: boolean;
    /** Проверено на дубли (установлен ИНН). */
    duplicateChecked: boolean;
    /** Найдены дубли. */
    duplicateFound: boolean;
    /** Присоединён к существующей работе. */
    mergedByExist: boolean;
    /** Повлиял на продажу. */
    boostSale: boolean;
    /** История обработки заявки (append-only, старые не переписываются). */
    history: string[];
    /**
     * Связанная основная сделка (to_base_sales).
     * @nullable
     */
    baseDealId?: number | null;
    /**
     * Связанная ХО-сделка (to_xo_sales).
     * @nullable
     */
    xoDealId?: number | null;
    /** Заявка принята менеджером после последнего назначения (назначение ≠ принятие). false — показать кнопку «Принять в работу». */
    isAccepted: boolean;
    /**
     * Кому назначен лид (ASSIGNED_BY_ID). Фронт сравнивает с текущим пользователем: непринятая ЧУЖАЯ заявка — кнопки принятия не показываются (после передачи принимает новый ответственный, а не прежний).
     * @nullable
     */
    assignedById?: number | null;
    /** Готовность к фиксации продажи (что ещё не отмечено). */
    saleReadiness: LeadRequestSaleReadinessDto;
    /** Предупреждения (неустановленные поля и т.п.). */
    warnings: string[];
}
