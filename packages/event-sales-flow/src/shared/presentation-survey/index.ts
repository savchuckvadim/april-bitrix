/**
 * Анкета «5К/Хвост» после презентации — ОБЩЕЕ место для всех её писателей.
 *
 * Здесь живут коды полей (жёсткий whitelist) и нормализация ответов.
 * Пользуются оба входа:
 *  - основной поток event-report — ответы приезжают в payload отчёта
 *    (`presentation.survey`) и пишутся в лид/сделки/компанию тем же
 *    батчем, что и сам отчёт;
 *  - легаси-ручка `/event-sales/presentation-survey` — мост для старого
 *    React-фронта, который шлёт анкету отдельным запросом.
 *
 * Модуля Nest здесь нет намеренно: только константы и чистые функции.
 */
export * from './presentation-survey.codes';
export * from './presentation-survey.values';
