/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * pbx-install API
 * API приложения pbx-install
 * OpenAPI spec version: 1.0
 */
import type { StageDto } from './stageDto';

export interface CategoryDto {
  /** Идентификатор категории из Excel-файла установки. Уникален в пределах книги и используется для связи со стадиями. */
  id: string;
  /** entityTypeId категории в Bitrix CRM. Для базовой сделки — `2`, для смарт-процессов — динамический. */
  entityTypeId: string;
  /** Тип сущности Bitrix, к которой относится категория (например, `deal`). */
  entityType: string;
  /** Тип воронки в схеме установки (например, `base` для базовой воронки). */
  type: string;
  /** Группа отдела, к которой относится категория. Совпадает со значением `:group` в URL эндпоинта. */
  group: string;
  /** Отображаемое название категории (воронки) в Bitrix. */
  name: string;
  /** Заголовок категории. По соглашению дублирует `name` для совместимости с местами, где используется `title`. */
  title: string;
  /** CATEGORY_ID воронки в Bitrix CRM. На этапе парсинга Excel заполняется пустой строкой и проставляется при установке. */
  bitrixId: string;
  /** CamelCase-представление `bitrixId` для маппинга на ключи запросов Bitrix. На этапе парсинга пустая строка. */
  bitrixCamelId: string;
  /** Внутренний код категории в приложении April (одно из значений `ParseCategoryNameEnum`). */
  code: string;
  /** Признак активности категории. `true` — используется при установке, `false` — пропускается. */
  isActive: boolean;
  /** Признак того, что категория требует обновления при повторной установке. */
  isNeedUpdate: boolean;
  /**
   * Порядок сортировки категории в списке воронок. Целое неотрицательное число.
   * @minimum 0
   */
  order: number;
  /** Является ли категория дефолтной для соответствующего entityType. `true` — дефолтная воронка. */
  isDefault: boolean;
  /** Стадии, входящие в категорию. Для каждой категории отфильтрованы по `categoryId` в Excel-файле. */
  stages: StageDto[];
}
