/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * kpi-report-sales API
 * API приложения kpi-report-sales
 * OpenAPI spec version: 1.0
 */

export interface HotClientDealDto {
    /** ID сделки Bitrix24 — для перехода по ссылке в сделку. */
    id: number;
    /** Название сделки. */
    title: string;
    /** ID ответственного (ASSIGNED_BY_ID). */
    assignedId: number;
    /** Код стадии воронки (из лестницы sales_base). */
    stageCode: string;
    /** Название стадии на портале. */
    stageName: string;
    /** Сумма сделки (OPPORTUNITY). */
    opportunity: number;
    /** Σ цена × количество по товарным строкам сделки. */
    productRowsAmount: number;
    /** Сумма товарных строк, приведённая к одному месяцу: Σ (сумма строки / месяцы строки по единице измерения). */
    monthlyAmount: number;
    /** Потенциально оплаченных месяцев: Σ количество × коэффициент из единицы измерения (сколько месяцев закроется при выигрыше). */
    paidMonths: number;
    /** Количество товара (Σ количество по товарным строкам, без коэффициентов). */
    quantity: number;
    /**
     * Дата начала действия договора (pbx contract_start), ISO; null — не заполнена.
     * @nullable
     */
    contractStart: string | null;
    /**
     * Дата окончания действия договора (pbx contract_end), ISO; null — не заполнена.
     * @nullable
     */
    contractEnd: string | null;
    /**
     * Код типа договора (XML_ID элемента живого словаря; bx_<ID> для элементов без XML_ID); null — не задан/поле не настроено.
     * @nullable
     */
    contractTypeCode: string | null;
    /**
     * Название типа договора (VALUE живого словаря — включает типы, добавленные на портале руками); null — не задан.
     * @nullable
     */
    contractTypeName: string | null;
    /** Значения поля «ОП История» (op_history). */
    opHistory: string[];
    /** Значения multiple-поля «ОП История (Комментарии)» (op_mhistory); пустой массив — не заполнено/поле не настроено. */
    opMHistory: string[];
    /** Комментарии презентаций (поле pres_comments). */
    comments: string[];
    /**
     * ID компании сделки (COMPANY_ID); null, если компания не привязана.
     * @nullable
     */
    companyId: number | null;
    /**
     * Название компании сделки (TITLE); null, если компания не привязана.
     * @nullable
     */
    companyName: string | null;
    /**
     * Перспектива компании (pbx op_prospects, code = цвет: green/yellow/red…); null — не задана.
     * @nullable
     */
    companyColor: string | null;
    /**
     * Тип клиента компании (pbx op_client_type: state/commerc/ip/fiz/layer); null — не задан.
     * @nullable
     */
    companyClientType: string | null;
}
