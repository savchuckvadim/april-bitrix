/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * kpi-report-sales API
 * API приложения kpi-report-sales
 * OpenAPI spec version: 1.0
 */

export interface ClosedSalesDealDto {
    /** ID сделки Bitrix24 (для ссылки на портал). */
    id: number;
    /** Название сделки. */
    title: string;
    /** ID ответственного (ASSIGNED_BY_ID). */
    assignedId: number;
    /** Дата закрытия сделки (CLOSEDATE), ISO. */
    closeDate: string;
    /** Сумма сделки (OPPORTUNITY). */
    opportunity: number;
    /** Аванс (предоплата): деньги, которые уже в сделке — Σ цена × количество по товарным строкам. */
    advanceAmount: number;
    /** Оплачено месяцев: Σ количество × коэффициент из единицы измерения (например «лиц.12мес.» → 12). */
    paidMonths: number;
    /** Сумма сделки, приведённая к одному месяцу: Σ (сумма строки / месяцы строки). */
    monthlyAmount: number;
    /** Количество товара (Σ количество по товарным строкам, без коэффициентов). */
    quantity: number;
    /**
     * Дата начала действия договора (поле contract_start), ISO; null, если не заполнена.
     * @nullable
     */
    contractStart: string | null;
    /**
     * Дата окончания действия договора (поле contract_end), ISO; null, если не заполнена.
     * @nullable
     */
    contractEnd: string | null;
    /** Длительность договора в месяцах (по датам с/по, округление по половине месяца). */
    contractMonths: number;
    /**
     * Код типа договора (XML_ID элемента живого словаря; bx_<ID> для элементов без XML_ID); null — не задан/поле не настроено.
     * @nullable
     */
    contractTypeCode: string | null;
    /**
     * Название типа договора (VALUE живого словаря — включает типы, добавленные на портале руками); null — не задан.
     * @nullable
     */
    contractTypeName: string | null;
    /** Ожидаемая сумма за весь договор: месячная сумма × месяцы договора. */
    expectedContractAmount: number;
    /**
     * ID компании сделки (COMPANY_ID); null, если компания не привязана.
     * @nullable
     */
    companyId: number | null;
    /**
     * Название компании сделки (TITLE); null, если компания не привязана.
     * @nullable
     */
    companyName: string | null;
    /**
     * Перспектива компании (pbx op_prospects, code = цвет: green/yellow/red…); null — не задана.
     * @nullable
     */
    companyColor: string | null;
    /**
     * Тип клиента компании (pbx op_client_type: state/commerc/ip/fiz/layer); null — не задан.
     * @nullable
     */
    companyClientType: string | null;
}
