/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * kpi-report-sales API
 * API приложения kpi-report-sales
 * OpenAPI spec version: 1.0
 */

export interface AiAttentionRequestDto {
    /** Домен портала Bitrix24. */
    domain: string;
    /** Bitrix-id пользователя, запрашивающего данные (requester). По нему определяется периметр видимости: руководитель — все/отдел/группа, менеджер — только свои строки. */
    requesterUserId: string;
    /** Начало периода (YYYY-MM-DD, TZ портала), включительно. По умолчанию фронт берёт скользящие 4 недели. */
    from: string;
    /** Конец периода (YYYY-MM-DD, TZ портала), включительно. Период не длиннее 3 месяцев и from ≤ to. */
    to: string;
    /** Bitrix-id менеджеров. Пусто — весь ростер отделов продаж по структуре. Список нормализуется (дедуп, сортировка) и входит в ключ результата. */
    managerIds?: number[];
    /** Только подтверждённые события (сшивка разбора с самоотчётом). В Фазе 1b принимается и входит в ключ результата, но фильтрация не применяется — сшивка по сделке появится в Фазе 3. */
    confirmedOnly?: boolean;
    /** ID WebSocket-соединения клиента: при расчёте в очереди результат придёт событием ai-analytics:overview:done (ошибка — ai-analytics:overview:error). */
    socketId?: string;
    /** Пересчитать обзор, игнорируя кэш чтения. Свежий результат перезапишет кэш (write-through). Идущий расчёт не дублируется. */
    forceRefresh?: boolean;
}
