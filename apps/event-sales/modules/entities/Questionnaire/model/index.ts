/**
 * Типы слайса: доменные (разобранный каталог) и сырые DTO контракта бэка.
 *
 * Потребители снаружи берут ДОМЕННЫЕ типы; сырые (сгенерированные orval'ом и
 * их сетевое расширение `*Wire`) нужны только хелперу и нормализатору.
 */
export * from './questionnaire.type';
export * from './questionnaire-dto.type';
