/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * event-sales API
 * API приложения event-sales
 * OpenAPI spec version: 1.0
 */
import type { CallReportAnalyticsQueryDtoCallType } from './callReportAnalyticsQueryDtoCallType';

export interface CallReportAnalyticsQueryDto {
    /** Домен портала Bitrix24, по которому строится отчёт. */
    domain: string;
    /** Начало периода отчёта (ISO 8601, по времени звонка). */
    from: string;
    /** Конец периода отчёта (ISO 8601, по времени звонка). */
    to: string;
    /** Bitrix-id менеджера (ответственного сделки) НА ЭТОМ ПОРТАЛЕ — НЕ id пользователя нашей БД. Id уникален только в связке с domain (на разных порталах id совпадают), поэтому фильтр всегда работает внутри обязательного domain. Звонки без сохранённого менеджера (обработанные до включения фичи) при фильтре отбрасываются — их число видно в meta.skippedNoManager. */
    managerId?: string;
    /**
     * Минимальная длительность звонка, сек (включительно).
     * @minimum 0
     */
    minDurationSec?: number;
    /**
     * Максимальная длительность звонка, сек (включительно).
     * @minimum 0
     */
    maxDurationSec?: number;
    /** Фильтр по типу звонка (коды CALL_TYPE смарта). Тип берётся из анализа агента, иначе — из дешёвого классификатора конвейера. */
    callType?: CallReportAnalyticsQueryDtoCallType;
    /** Сохранить построенный отчёт снапшотом в историю (таблица ais, type=report-<вид>). Нужно для трендов и сравнения периодов: история хранит отчёт таким, каким он был на момент построения, даже если звонки потом переразобраны. Разовые ad-hoc запросы историю не засоряют — по умолчанию false. */
    saveToHistory?: boolean;
    /** Использовать Redis-кэш. true (по умолчанию) — если по ТЕМ ЖЕ параметрам (домен+период+все фильтры) отчёт уже строился и не протух (TTL 1 час, env CALL_REPORT_ANALYTICS_CACHE_TTL_SEC) — отдаётся мгновенно из кэша, meta.fromCache=true. false — принудительный пересчёт из БД (forceRefresh), результат перезаписывает кэш. Полный сброс — POST cache/reset. */
    useCache?: boolean;
}
