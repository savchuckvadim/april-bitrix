/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * event-sales API
 * API приложения event-sales
 * OpenAPI spec version: 1.0
 */

export interface TransferWorkRunDto {
    /** Домен портала Bitrix. По нему PBXService отдаёт инстанс Bitrix с ключами доступа портала. */
    domain: string;
    /** Идентификатор операции от клиента. Повторный POST с тем же operationId вернёт уже существующую операцию, не выполняя хук второй раз. Если не передан — сгенерируется сервером. */
    operationId?: string;
    /** socketId WebSocket-подключения фрейма — для push-уведомления о завершении (события sales-hook:done / sales-hook:error). Без него фрейм просто поллит статус. */
    socketId?: string;
    /**
     * Идентификатор пользователя Bitrix, инициировавшего операцию.
     * @minimum 1
     */
    initiatorUserId?: number;
    /**
     * Компания, чья работа передаётся (со всеми открытыми сделками наших воронок). Обязательна, если не переданы dealIds.
     * @minimum 1
     */
    companyId?: number;
    /** Явный набор передаваемых сделок. Обязателен, если не передана companyId. Чужие воронки игнорируются. */
    dealIds?: number[];
    /**
     * Новый ответственный менеджер.
     * @minimum 1
     */
    newResponsibleId: number;
    /** Передавать ли просроченные задачи (по умолчанию да — новый менеджер должен видеть, на когда была договорённость). */
    includeOverdue?: boolean;
    /** Перепланировать просроченные дедлайны от даты передачи (по умолчанию нет — просрочка сохраняется). */
    rescheduleOverdue?: boolean;
    /** Перевести основную сделку в стадию «Холодная» при передаче (по умолчанию стадия не меняется). */
    moveMainDealToCold?: boolean;
    /** Создать новому ответственному задачу «Звонок». */
    createCallTask?: boolean;
}
