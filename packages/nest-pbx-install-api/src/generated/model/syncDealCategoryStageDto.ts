/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * pbx-install API
 * API приложения pbx-install
 * OpenAPI spec version: 1.0
 */
import type { SyncDealCategoryStageDtoGroup } from './syncDealCategoryStageDtoGroup';
import type { SyncDealCategoryStageDtoCategoryCode } from './syncDealCategoryStageDtoCategoryCode';

export interface SyncDealCategoryStageDto {
  /** Домен Bitrix-портала без протокола и завершающего слэша. Передайте "all", чтобы выполнить операцию для всех порталов. */
  domain: string;
  /** Группа шаблона — определяет Excel-файл, из которого читается строка стадии (`install/{group}/deal/data.xlsx`). */
  group: SyncDealCategoryStageDtoGroup;
  /** `code` воронки-владельца стадии. Воронка обязана уже существовать на портале: поштучная синхронизация стадии её не создаёт. */
  categoryCode: SyncDealCategoryStageDtoCategoryCode;
  /** `code` стадии из шаблона. Все атрибуты (название, цвет, `bitrixId`, семантика, порядок) берутся из строки шаблона, а не из запроса — чтобы портал и шаблон не разъезжались. */
  stageCode: string;
  /** Пересчитать SORT остальных стадий воронки по шаблону. Включено по умолчанию: стадия, вставленная в СЕРЕДИНУ лестницы, иначе встанет в Bitrix последней — у соседей остаются старые SORT. Правится только порядок: названия, цвета и семантика соседних стадий не трогаются. Передайте `false`, если порядок в воронке настроен руками и его нельзя перетряхивать. */
  reorder?: boolean;
}
