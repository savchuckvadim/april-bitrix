/**
 * Корень react-query-ключей слайса ЗПР: по нему WS-инвалидация
 * (`zpr-flow:done`) сбрасывает и ссылки, и элементы одним вызовом.
 * Словари стадий живут под своим ключом — их инвалидация не трогает
 * (воронка за сессию не меняется).
 */
export const ZPR_QUERY_ROOT = 'zprCalls';

/** Ключ словарей стадий — вне корня инвалидации. */
export const ZPR_STAGES_QUERY_ROOT = 'zprStages';
