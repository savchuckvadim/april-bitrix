/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * event-sales API
 * API приложения event-sales
 * OpenAPI spec version: 1.0
 */

export interface BitrixTranscriptionRequestDto {
    /** Прямой URL аудиофайла, который нужно расшифровать. Должен быть доступен извне (например DOWNLOAD_URL из Bitrix24). */
    fileUrl: string;
    /** Имя файла, используется в логах и для расшифровки. */
    fileName: string;
    /** ID пользователя Bitrix24, который инициировал транскрибацию. */
    userId: string;
    /** Имя пользователя Bitrix24 (для логов и истории). */
    userName: string;
    /** Имя приложения-источника (для группировки в истории). */
    appName: string;
    /** ID активности Bitrix24 (CRM activity), к которой относится файл. */
    activityId: string;
    /** ID файла в Bitrix24 Disk / FILES активности. */
    fileId: string;
    /** Длительность аудиофайла в секундах (строкой, для совместимости с фронтом). */
    duration: string;
    /** Отдел или подразделение пользователя (для группировки в истории). */
    department: string;
    /** Тип CRM-сущности (deal / lead / company), к которой привязан звонок. */
    entityType: string;
    /** ID связанной CRM-сущности в Bitrix24. */
    entityId: string;
    /** Имя CRM-сущности (название сделки / лида), для удобного отображения в истории. */
    entityName: string;
    /** Домен портала Bitrix24, на котором лежит файл. */
    domain: string;
}
