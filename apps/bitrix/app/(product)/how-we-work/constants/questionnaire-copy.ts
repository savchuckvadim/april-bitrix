/**
 * Подписи движка анкет, которых нет в самих анкетах: пометка обязательного
 * вопроса и текст ошибки, когда обязательный ответ пуст.
 *
 * Тексты движка живут здесь, а не в компонентах: в `.tsx` контента быть не
 * должно, а один и тот же текст нужен и форме, и проверке перед отправкой.
 */
export const HOW_QUESTIONNAIRE_COPY = {
    /** Пометка рядом с заголовком обязательного вопроса. */
    requiredMark: 'обязательно',
    /** Ошибка отправки: не отвечен обязательный вопрос. */
    requiredError: (title: string): string =>
        `Ответьте на обязательный вопрос: «${title}»`,
} as const;
