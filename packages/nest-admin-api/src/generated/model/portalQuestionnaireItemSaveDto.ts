/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * admin API
 * API приложения admin
 * OpenAPI spec version: 1.0
 */
import type { PortalQuestionnaireItemSaveDtoControl } from './portalQuestionnaireItemSaveDtoControl';
import type { PortalQuestionnaireItemSaveDtoChannel } from './portalQuestionnaireItemSaveDtoChannel';
import type { PortalQuestionnaireItemSaveDtoTargetMode } from './portalQuestionnaireItemSaveDtoTargetMode';
import type { PortalQuestionnaireItemSaveDtoTargetEntity } from './portalQuestionnaireItemSaveDtoTargetEntity';
import type { PortalQuestionnaireItemSaveDtoFieldSource } from './portalQuestionnaireItemSaveDtoFieldSource';
import type { PortalQuestionnaireItemSaveDtoFieldStatus } from './portalQuestionnaireItemSaveDtoFieldStatus';
import type { PortalQuestionnaireItemSaveDtoMeta } from './portalQuestionnaireItemSaveDtoMeta';
import type { PortalQuestionnaireOptionSaveDto } from './portalQuestionnaireOptionSaveDto';

export interface PortalQuestionnaireItemSaveDto {
  /** Код ВОПРОСА (не поля): ключ ответа во фрейме. Два вопроса могут писать в одно поле и не делить между собой значение. */
  code: string;
  /** Формулировка вопроса менеджеру. */
  title: string;
  /**
   * Подсказка внутри поля ввода.
   * @nullable
   */
  placeholder?: string | null;
  /**
   * Пояснение под контролом.
   * @nullable
   */
  hint?: string | null;
  /**
   * Заголовок секции внутри анкеты.
   * @nullable
   */
  groupTitle?: string | null;
  /** Порядок вопроса в анкете. */
  sort?: number;
  /** Тип отображения. Должен быть совместим с типом поля по матрице реестра — иначе сохранение отклоняется. */
  control: PortalQuestionnaireItemSaveDtoControl;
  /** Множественное поле. В этой версии запрещено: запись массивов во фрейме не реализована, ответы исчезали бы бесследно. */
  isMultiple?: boolean;
  /** Обязательный вопрос блокирует отправку отчёта. */
  isRequired?: boolean;
  /** Закрывается только ответом этой сессии: значение, уже стоящее в CRM, не считается. Только для канала «Поле CRM». */
  requireChange?: boolean;
  /**
   * Срок годности ответа в днях: значение старше перестаёт закрывать обязательный вопрос. Только для типов «Дата» и «Дата и время».
   * @nullable
   */
  staleAfterDays?: number | null;
  /** Куда пишется ответ. */
  channel?: PortalQuestionnaireItemSaveDtoChannel;
  /** Как выбирается носитель ответа. */
  targetMode?: PortalQuestionnaireItemSaveDtoTargetMode;
  /**
   * Сущность-носитель — только при жёстком выборе.
   * @nullable
   */
  targetEntity?: PortalQuestionnaireItemSaveDtoTargetEntity;
  /**
   * Путь в отчёте для канала «Поле отчёта». Только из реестра `dtoPaths`.
   * @nullable
   */
  dtoPath?: string | null;
  /** Штатное поле Битрикса (OPPORTUNITY), не UF. */
  isNative?: boolean;
  /**
   * Полное имя поля РОВНО как его вернул Битрикс. Никогда не собирать конкатенацией с `UF_CRM_`.
   * @nullable
   */
  fieldName?: string | null;
  /**
   * id поля в Битриксе (вторичный якорь привязки).
   * @nullable
   */
  fieldBitrixId?: number | null;
  /**
   * xmlId поля. Недоступен, если у ключа портала нет прав администратора CRM.
   * @nullable
   */
  fieldXmlId?: string | null;
  /**
   * Наш код из pbx-реестра — если поле ставили мы.
   * @nullable
   */
  fieldCode?: string | null;
  /**
   * `userTypeId` поля на момент привязки: по нему проверяется матрица и ловится смена типа.
   * @nullable
   */
  fieldType?: string | null;
  /** Носитель, ИЗ КОТОРОГО поле выбрано в пикере. Обязателен для каналов «Поле CRM» и «Поле элемента смарта» с пользовательским полем: только он связывает поле с сущностью, куда уедет ответ. Поле смарта на канале «Поле CRM» отклоняется (фрейм в смарт не пишет), поле контакта требует жёсткого носителя — цепочка `auto` (компания → сделка → лид) до него не доходит. В БД не хранится: проверяется на сохранении, постоянный адрес смарта — в `smartId`. */
  fieldSource?: PortalQuestionnaireItemSaveDtoFieldSource;
  /**
   * Строка `smarts` НАШЕЙ БД (тот же идентификатор, что в `GET /questionnaire-fields/sources`). Обязателен для канала «Поле элемента смарта»: без него неизвестно, в элемент какого смарта писать ответ. В отличие от `fieldSource` — СОХРАНЯЕТСЯ: по нему компиляция каталога узнаёт поток события, а сверка привязок — где искать поле.
   * @nullable
   */
  smartId?: number | null;
  /** Состояние привязки (ставится проверкой привязок). */
  fieldStatus?: PortalQuestionnaireItemSaveDtoFieldStatus;
  /**
   * Расширения без миграций: min/max, rows, маска ввода.
   * @nullable
   */
  meta?: PortalQuestionnaireItemSaveDtoMeta;
  /** Вопрос гасим, а не удаляем: иначе уже собранные ответы в CRM становятся необъяснимыми. */
  isActive?: boolean;
  /** Варианты справочника — только для типа отображения «Список». */
  options?: PortalQuestionnaireOptionSaveDto[];
}
