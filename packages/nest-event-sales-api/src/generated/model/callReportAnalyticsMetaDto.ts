/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * event-sales API
 * API приложения event-sales
 * OpenAPI spec version: 1.0
 */
import type { CallReportAnalyticsMetaDtoFilters } from './callReportAnalyticsMetaDtoFilters';

export interface CallReportAnalyticsMetaDto {
    /** Вид отчёта. */
    report: string;
    /** Домен портала. */
    domain: string;
    /** Начало периода (ISO). */
    from: string;
    /** Конец периода (ISO). */
    to: string;
    /** Применённые фильтры (эхо запроса). */
    filters: CallReportAnalyticsMetaDtoFilters;
    /** Всего звонков конвейера в периоде (до фильтров). */
    totalCalls: number;
    /** Звонков после применения фильтров — база отчёта. */
    filteredCalls: number;
    /** Из них с глубоким анализом агента (agent-analysis) — по ним считаются оценки/разделы/возражения. */
    analyzedCalls: number;
    /** Отброшено фильтром по менеджеру из-за отсутствия менеджера в строке (звонки, обработанные до включения сохранения менеджера). */
    skippedNoManager: number;
    /** Отчёт отдан из кэша (true) или пересчитан (false). */
    fromCache: boolean;
    /** Момент построения отчёта (ISO). */
    generatedAt: string;
    /**
     * ID записи истории в ais (если запрошено saveToHistory). null — сохранение не запрашивалось или не удалось.
     * @nullable
     */
    historyId?: string | null;
}
