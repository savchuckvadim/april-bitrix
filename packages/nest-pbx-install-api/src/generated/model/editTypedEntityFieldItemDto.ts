/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * pbx-install API
 * API приложения pbx-install
 * OpenAPI spec version: 1.0
 */

export interface EditTypedEntityFieldItemDto {
  /** Домен Bitrix-портала без протокола и завершающего слэша. Передайте "all", чтобы выполнить операцию для всех порталов. */
  domain: string;
  /** Code поля-энумератора (например, "contractType"). */
  fieldCode: string;
  /** Code редактируемого элемента списка. По нему находится запись в PortalDB; непосредственный update в Bitrix выполняется по `bitrixfield_items.bitrixId`, хранящемуся в PortalDB после установки поля. */
  itemCode: string;
  /** Новое отображаемое значение item-а (`VALUE` в Bitrix, `name`/`title` в PortalDB). Code остаётся прежним. */
  newValue: string;
  /** Тип типизированной сущности (например, `presentation` для смарта или код RPA). Совпадает с тем, что хранится в `smarts.type` / `btx_rpa.type` и используется при первичной установке. */
  type: string;
  /** Группа отдела/приложения, в которой живёт сущность (например, `sales`). Совпадает с `smarts.group` / `btx_rpa.group`. */
  group: string;
}
