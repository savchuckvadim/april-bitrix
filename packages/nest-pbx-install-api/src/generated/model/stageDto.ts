/**
 * Generated by orval v7.13.0 🍺
 * Do not edit manually.
 * pbx-install API
 * API приложения pbx-install
 * OpenAPI spec version: 1.0
 */
import type { StageDtoType } from './stageDtoType';
import type { StageDtoIsDefault } from './stageDtoIsDefault';

export interface StageDto {
  /** Идентификатор стадии из Excel-файла установки. Уникален в пределах книги и используется для связи со стадиями. */
  id: string;
  /** entityTypeId стадии в Bitrix CRM (для сделок и смарт-процессов). Наследуется от родительской категории. */
  entityTypeId: string;
  /** Тип сущности Bitrix, к которой относится стадия (например, `deal`). */
  entityType: string;
  /** Код приложения-владельца стадии (parentType из Excel). Например, `service` для отдела сервиса. */
  parentType: string;
  /** Семантика стадии Bitrix: `P` — основная (in progress), `S` — успешная (success), `F` — провальная (failure). */
  type: StageDtoType;
  /** Группа отдела, к которой относится стадия (`sales` или `service`). Наследуется от родительской категории. */
  group: string;
  /** Отображаемое название стадии в карточке Bitrix. */
  name: string;
  /** Заголовок стадии. По соглашению дублирует `name` для совместимости с местами, где используется `title`. */
  title: string;
  /** STATUS_ID стадии в Bitrix (например, `NEW`, `SUPPLY`). Используется как стабильный идентификатор стадии в портале. */
  bitrixId: string;
  /** Признак активности стадии. `true` — стадия используется при установке, `false` — пропускается. */
  isActive: boolean;
  /** STATUS_ID стадии для смарт-процесса (если стадия дублируется в смарте). Пустая строка для базовых сделок. */
  smartBitrixId: string;
  /** HEX-код цвета стадии в карточке Bitrix (вместе с `#`). */
  color: string;
  /** Внутренний код стадии в приложении April. Используется как стабильный ключ для маппинга на стадии Bitrix. */
  code: string;
  /** Признак того, что стадия требует обновления при повторной установке. `true` — переустанавливать/синхронизировать, `false` — пропускать. */
  isNeedUpdate: boolean;
  /**
   * Порядок сортировки стадии в воронке. Целое неотрицательное число.
   * @minimum 0
   */
  order: number;
  /** ENTITY_ID стадии в Bitrix CRM. Заполняется в процессе установки, на входе обычно пустая строка. */
  bitrixEnitiyId: string;
  /** Является ли стадия дефолтной для своей категории. `Y` — дефолтная, `N` — нет. */
  isDefault: StageDtoIsDefault;
}
