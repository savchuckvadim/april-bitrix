/**
 * Палитры эффектов вкладки «AI аналитика» (данные отдельно от UI).
 *
 * Hex здесь — осознанно: это параметры градиента и WebGL-шейдера, а не
 * цвета интерфейса (тот же принцип, что у feedback/Processing/processing-fx).
 */

/** Градиент заголовка вкладки (фиолет → циан → фиолет). */
export const AI_TITLE_GRADIENT = ['#bb52d4', '#30c3ef', '#7c5cff', '#bb52d4'];

/** Стопы северного сияния под шапкой вкладки. */
export const AI_AURORA_COLOR_STOPS = ['#7c5cff', '#30c3ef', '#bb52d4'];

/** Затухание сияния при появлении, мс. */
export const AI_AURORA_FADE_MS = 1200;
