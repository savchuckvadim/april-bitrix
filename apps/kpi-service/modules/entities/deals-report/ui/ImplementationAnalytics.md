# ImplementationAnalytics - Анализ динамики реализации

Компонент для анализа динамики реализации и индексации сделок по компаниям.

## Основные возможности

### 1. Индекс реализации (Implementation Index)
- Отслеживает изменение суммы сделок по каждому клиенту со временем
- Рассчитывает процент изменения между последовательными сделками
- Формула: `delta = (current.sum - previous.sum) / previous.sum * 100`

### 2. Реализация (Realization)
- Учитывает статус сделок:
  - `isWon` или `isInProgress` = успешная (сумма добавляется к выручке)
  - `isLost` = отказ (сумма не учитывается)
- Рассчитывает общую выручку от успешных сделок в рублях

## Компоненты

### AverageDealSumTrendChart
**Линейный график изменения средней суммы сделок во времени**
- Ось X: месяцы
- Ось Y: средняя сумма сделки (₽)
- Дополнительно: количество сделок (столбцы)

### MonthlyIndexChart
**Столбчатая диаграмма средней индексации по месяцам**
- Ось X: месяцы
- Ось Y: средняя индексация (%)
- Дополнительно: процент реализации (%)

### CompanyRealizationHeatmap
**Тепловая карта выручки от реализации по компаниям и периодам**
- Вертикально: компании (топ-10)
- Горизонтально: кварталы
- Цветовая индикация:
  - **Индексация**: зеленый (рост >10%), желтый (±10%), красный (падение >10%)
  - **Выручка от реализации**: зеленый (>1М ₽), желтый (500К-1М ₽), красный (<500К ₽)

### ImplementationSummary
**Сводная аналитика**
- Общее количество компаний
- Распределение по категориям роста
- Средняя индексация
- Общая выручка от реализации
- Количество успешных сделок

## Использование

```tsx
import { ImplementationAnalytics } from './ImplementationAnalytics';

// В компоненте
<ImplementationAnalytics companies={companiesData} />
```

## Типы данных

### OrkReportDealsByCompaniesDto
```typescript
interface OrkReportDealsByCompaniesDto {
    deals: OrkReportDealItemDto[];
    company: OrkReportCompanyItemDto;
}
```

### OrkReportDealItemDto
```typescript
interface OrkReportDealItemDto {
    id: number;
    title: string;
    sum: string;
    from: string;
    to: string;
    isWon: boolean;
    isLost: boolean;
    isInProgress: boolean;
    // ... другие поля
}
```

## Алгоритм расчета индексации

1. **Сортировка сделок** по дате (`from`)
2. **Расчет изменения** для каждой сделки относительно предыдущей
3. **Усреднение** по периодам (месяцы/кварталы)
4. **Классификация компаний**:
   - Растущие: рост > 10%
   - Стабильные: ±10%
   - Падающие: падение > 10%

## Цветовая схема

- 🟢 **Зеленый**: положительные показатели (рост, высокая выручка от реализации)
- 🟡 **Желтый**: нейтральные/средние показатели
- 🔴 **Красный**: отрицательные показатели (падение, низкая выручка от реализации)
